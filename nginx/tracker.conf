server {
    listen 80;
    server_name yourdomain.com;

    location /rtag.min.js {
        root /var/www/tracker/public;
    }

    location /collect {
        proxy_pass http://localhost:3000/collect;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
